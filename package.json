{
  "name": "local_db",
  "version": "1.0.0",
  "description": "\"A local version of mongodb in a JSON file, smae features as mongoose\"",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm link && tsc",
    "dev": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "a"
  },
  "author": "CodeByNavin",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.0.7",
    "typescript": "^5.8.3"
  }
}
